# Schutz: niemals Secrets oder lizenzierte Daten in dieses ÖFFENTLICHE Repo.
# Dieses Repo enthält ausschließlich das statische Frontend (index.html).
.hf_token
.ACCESS_KEY
*.token
*.key
.env
.env.*

# lizenzierte Daten / abgeleitete Mappings (gehören NICHT ins öffentliche Repo)
*.parquet
*.csv
*.duckdb
manifest.json
tickers.json
permnos.json

# OS-/Editor-Müll
Thumbs.db
Desktop.ini
.DS_Store
*.swp
